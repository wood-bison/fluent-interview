# Gate W15 — PASS WITH PERFORMANCE/REPOSITORY-HYGIENE DEBT

The generated Vue artifact has an inspectable Vite manifest. Initial learner
routes stay below the recorded JS/CSS budgets, xterm remains lazy, all
referenced assets exist, and Question Brain pagination is bounded. The Vue
owner check and 76 desktop E2E scenarios are green.

The wave is partial until interaction budgets, dependency deprecations,
evidence-retention policy, fresh-clone size and cache ownership are independently
verified. This gate does not make the dirty multi-repository workspace a
production package.

Evidence: `vue-performance.json`, `tests.json`, Vue build output and W13 E2E.
