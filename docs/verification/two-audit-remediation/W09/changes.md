# W09 changes

- Added `track-view-isolation-gate.mjs`, a read-only comparison of the Lab
  Program/Map projection and the published Brain catalogue.
- The gate verifies all nine canonical paths, exact path counters, accepted vs
  proposed semantics, canonical `/paths/:slug` entry routes, shared-domain
  references, and dedicated Algorithms/Behavioral isolation.
- Integrated the gate into the root development release verifier. It does not
  make route decisions in the browser and does not mutate learner progress.
