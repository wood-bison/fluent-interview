# W01 changes

- Added exact current `revision` pins for all five product roots to
  `workspace.yaml`.
- Corrected `docs/WORKSPACE-TOPOLOGY.md` to reflect Vault's local `main` and
  the still-open Vue remote provenance gap.
- No sibling directory was deleted and no unrelated container or repository was
  modified.
