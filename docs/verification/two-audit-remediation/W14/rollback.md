# W14 rollback

The gate and documentation are read-only. If a token migration causes a
visual regression, revert only the token consumer or scale addition after
running the Vue owner check. Do not delete releases, evidence, Docker volumes
or compatibility aliases to make the audit green.
