# Gate W11 — PARTIAL PASS (runtime identity and cross-profile outcome boundary reconciled)

The raw Task Runtime catalogue and family projection now reconcile against the
published Brain release with immutable identities, digest-pinned images and
network isolation. The gate does not yet claim that every adversarial runtime
outcome has been exercised; that remains the next runtime matrix wave.

Reports: `runtime-binding.json`, `runtime-release-join.json`,
`runtime-failure-matrix.json`

The live matrix uses disposable Runtime executions and a static Docker policy
regression. It proves exact-revision refusal plus pass verdicts for Node, Go,
Java, .NET and PostgreSQL, and proves Node assertion failure, compile/load
failure, timeout and private-diagnostic redaction without writing Lab progress.
It also finds the exported Jaeger `task.run` span and matches correlation, task
and exact revision identity. Retry/idempotency, broader negative cases, and an
immutable production Runtime release remain open work.
