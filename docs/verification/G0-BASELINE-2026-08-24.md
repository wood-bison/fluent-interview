# G0 baseline and recovery evidence — 2026-08-24

Status: **partial — local recovery proven; private vault remote is still missing**.

This record is deliberately limited to operational metadata. Database dumps and
the vault bundle stay outside Git in a machine-local temporary directory; no
learner content, prompts, credentials, or embeddings are committed here.

## Live baseline

Captured from the running workspace before changing the Question/Capability/Task
model:

| Boundary | Evidence |
| --- | --- |
| Question Brain | `http://127.0.0.1:48127/health/ready` → HTTP 200 |
| Task Runtime | `http://127.0.0.1:48227/v1/health/ready` → HTTP 200 |
| Fluent Lab web | `http://localhost:47300/` → HTTP 200 |
| Lab package | `http://localhost:49300/onboarding` → HTTP 200 |
| Question Brain release | `question-release-d550846f4743c4d3` |
| Runtime release | `runtime-task-release-2026-08-24-qb-d550846-i2` |
| Question production cards | 1,591 published; 1,591 mapped; 0 unmapped |
| Question embeddings | 10,653 |
| Lab graph | 817 nodes; 1,115 edges; `valid: true` |

The current workspace has one pre-existing user change outside this G0 record:
`docs/reports/architecture-audit-2026-08-24.html`. It was not staged or
modified by this gate.

## Backup artifacts

The artifacts were created in:

```text
/tmp/fluent-interview-g0-7Cdqz9/
```

The directory contains:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| `question-brain.dump` | 96 MiB | `2bfe25dc5086468fb48f037e3530fdd0b295ee8cc0f6de083fd2db17802551ed` |
| `fluent-lab.dump` | 8.3 MiB | `410a459b93a898a9482502cbf30584325307ea617cdcbeabc35712c183956917` |
| `fluent-lab-redis.rdb` | 4 KiB | `9aafab46e1cb51ec6ae595a04f65c6d5c525ae34a8227b91af3a65db7923c6bf` |
| `question-vault.bundle` | 7.2 MiB | `dbc63d37855f7c17ff6114a3a52bfc56720bcdfb73829332db16d1d8aa866fd3` |

The vault bundle was verified with `git bundle verify` and contains all refs
from the source vault branch. The Redis artifact passed `redis-check-rdb` with
`Checksum OK` and `RDB looks OK`.

## Restore drill

Both Postgres dumps were restored with `pg_restore --exit-on-error` into fresh,
isolated containers (not the running application containers):

```text
question_brain_restore=ok
published=1591
embeddings=10653
fluent_lab_restore=ok
tables=27
```

The temporary containers were removed after verification. No application
volume was removed and no `docker compose down -v` was used.

## G0 blocker

`/Users/sergeyzhechko/developer/fluent-question-vault` has no Git remote. The
plan requires a private, recoverable remote before G0 can be marked complete.
The existing GitHub namespace contains the three application repositories,
but `wood-bison/fluent-question-vault` does not currently exist. No remote was
created without an explicit destination, because guessing would risk pushing
the private source vault to the wrong account or visibility.

To close the remaining part of G0, provide/approve the private remote (for
example, an exact `git@...` URL). Then add it as `origin`, push the vault branch
and tags, and repeat `git ls-remote`, bundle verification, and the restore drill
against the remote ref.
