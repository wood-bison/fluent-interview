# W12 rollback

The route/action gate and projection are read-only at the release boundary.
Rollback means removing the W12 release-verifier step and reverting the Vue
contextual-action consumer plus exact selection query propagation as one
versioned change set. Keep the route reports and contract tests as audit
evidence; never restore the previous silent latest-revision fallback in a
production release.
