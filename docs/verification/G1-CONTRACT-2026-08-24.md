# G1 cross-system contract evidence — 2026-08-24

Status: **complete**.

## Contract artifacts

The canonical workspace contract is:

- `docs/contracts/question-capability-task.v1.schema.json`
- `docs/contracts/question-capability-task.v1.fixture.json`
- `docs/contracts/question-capability-task.v1.md`

The schema and fixture are mirrored into each child repository with identical
hashes:

```text
schema  b5363c033ac8e80eb2756a925a2fbd883f90a29e63562c72f9de3ade9c1baa7b
fixture ca6e1ca31998c7a1f02103925b382bd8abff2b427250b05dac544fdc364eff61
```

The fixture covers a theory-only card, Node-specific and PostgreSQL-specific
capabilities, a language-neutral rate limiter with Go/Java/C#/TypeScript
revisions, a circuit breaker under review, semantic relation provenance, a
multi-capability capstone, a Run, and Lab Evidence.

## Owner decisions

| Owner | Decision artifact | Implementation proof |
| --- | --- | --- |
| Question Brain | `fluent-question-brain/docs/adr/0003-reviewed-content-relations-and-capability-bindings.md` | `internal/contractmodel` validator + fixture test |
| Task Runtime | `fluent-task-runtime/docs/adr/0001-task-family-and-immutable-revisions.md` | `contracts/question_capability_task.go` validator + fixture test |
| Fluent Lab | `fluent-engineering-lab/docs/adr/0036-question-capability-task-composition.md` | `libs/lab-contracts/src/lib/question-capability-task.ts` + Jest tests |

The Lab glossary now defines Capability, domain binding, question-capability
binding, TaskFamily, QuestionBinding, Run, and Evidence. Existing ADR-0029,
ADR-0031, ADR-0032, and ADR-0035 remain compatible; the new ADR explains the
composition and does not reassign ownership.

## Verification

```text
Question Brain: make contract                                  PASS
Question Brain: docker golang:1.24 go test ./...                PASS
Task Runtime:  docker golang:1.24 go test ./...                PASS
Fluent Lab:    pnpm nx test lab-contracts --skip-nx-cache       PASS (241 suites, 1217 tests)
Fluent Lab:    pnpm nx build lab-contracts --skip-nx-cache      PASS
JSON:          python3 -m json.tool schema + fixture            PASS
```

Negative tests prove that a task-sequence capability key, a language/profile
field on TaskFamily, and a capability leaked into `questionKeys` are rejected.
Positive tests prove multiple domains, multiple capabilities, and an immutable
language-specific TaskRevision are accepted.

No database migration, release manifest, learner API response, or production
data changed in G1.

## Commits

- Question Brain `21a7b86` — `docs(contract): define reviewed capability and relation model`
- Task Runtime `53663ba` — `docs(contract): define task family and revision ownership`
- Fluent Lab `a098f36` — `docs(adr): define question capability task composition`

